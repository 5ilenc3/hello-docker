apiVersion: v1
kind: Pod
metadata:
  labels:
    run: hello-docker
  name: hello-docker
  namespace: challange
spec:
  containers:
  - image: harik8/hello-docker
    name: jnlp
    ports: 
      - name: jnlp
        containerPort: 80
        protocol: TCP
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Never
status: {}
